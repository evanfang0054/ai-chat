# Chat MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有鉴权与 RBAC 基础上交付可真实使用的 Chat MVP，支持多会话、消息持久化、DeepSeek 流式回复、类 ChatGPT Web 聊天页与用户隔离。

**Architecture:** 后端按 `chat -> run -> llm` 边界扩展：`chat` 负责会话/消息与 SSE 编排，`run` 负责组织一次聊天执行上下文，`llm` 负责 DeepSeek 流式调用。前端新增 `/chat` 页面、聊天状态 store、chat service 与组件树，先用最小状态模型实现会话列表、消息流、输入框、空态、错误态与流式渲染，不引入 tool、schedule 或 runtime 持久化。

**Tech Stack:** NestJS, Prisma, PostgreSQL, React, Vite, Zustand, React Router, Server-Sent Events, DeepSeek API, Vitest, Jest, Supertest

---

## File Structure Map

### Shared contract
- Create: `packages/shared/src/chat.ts` — 前后端共享的 chat session / message / SSE event / request-response 类型
- Modify: `packages/shared/src/index.ts` — 导出 chat 类型

### API app
- Modify: `apps/api/prisma/schema.prisma` — 新增 `ChatSession` / `ChatMessage` 与消息角色枚举
- Create: `apps/api/prisma/migrations/<timestamp>_add_chat_models/migration.sql` — 聊天表迁移
- Modify: `apps/api/src/app.module.ts` — 接入 `ChatModule`
- Modify: `apps/api/src/common/config/env.ts` — 增加 DeepSeek 环境变量校验与默认值
- Create: `apps/api/src/modules/chat/chat.module.ts` — chat 模块注册
- Create: `apps/api/src/modules/chat/chat.controller.ts` — 会话列表、消息历史、SSE 回复接口
- Create: `apps/api/src/modules/chat/chat.service.ts` — 会话/消息持久化、标题生成、用户隔离校验
- Create: `apps/api/src/modules/chat/dto/create-chat-message.dto.ts` — 发送消息 DTO
- Create: `apps/api/src/modules/chat/chat.types.ts` — chat 模块内部返回类型
- Create: `apps/api/src/modules/run/run.module.ts` — run 模块注册
- Create: `apps/api/src/modules/run/run.service.ts` — 历史消息转模型上下文并桥接 llm stream
- Create: `apps/api/src/modules/llm/llm.module.ts` — llm 模块注册
- Create: `apps/api/src/modules/llm/llm.service.ts` — DeepSeek 配置、请求组装、SSE/stream 解析
- Create: `apps/api/src/modules/llm/llm.types.ts` — llm stream 事件类型
- Create: `apps/api/test/chat.e2e-spec.ts` — chat API 与 SSE e2e
- Create: `apps/api/src/modules/run/run.service.spec.ts` — run 模块单测（mock llm）
- Modify: `apps/api/package.json` — 如需补充 DeepSeek SDK 或 markdown/sse 相关依赖脚本
- Modify: `.env.example` — 增加 `DEEPSEEK_API_KEY` / `DEEPSEEK_BASE_URL` / `DEEPSEEK_MODEL`

### Web app
- Modify: `apps/web/src/router/index.tsx` — 新增 `/chat` 路由并将 `/` 默认导向聊天页
- Modify: `apps/web/src/lib/api.ts` — 支持带 token 的请求头与非 JSON 响应
- Create: `apps/web/src/services/chat.ts` — 会话列表、历史消息、发送消息与 SSE 封装
- Create: `apps/web/src/stores/chat-store.ts` — 当前会话、临时消息、流式状态、错误态
- Create: `apps/web/src/pages/chat/ChatPage.tsx` — 聊天页入口
- Create: `apps/web/src/components/chat/SessionSidebar.tsx` — 左侧聊天侧栏
- Create: `apps/web/src/components/chat/SessionList.tsx` — 会话列表
- Create: `apps/web/src/components/chat/MessageList.tsx` — 消息列表
- Create: `apps/web/src/components/chat/MessageItem.tsx` — 单条消息与 Markdown 渲染
- Create: `apps/web/src/components/chat/ChatComposer.tsx` — 输入框与发送按钮
- Create: `apps/web/src/components/chat/EmptyChatState.tsx` — 空态
- Create: `apps/web/src/__tests__/chat-store.test.ts` — chat store 单测
- Create: `apps/web/src/__tests__/chat-page.test.tsx` — `/chat` 路由与页面行为测试
- Modify: `apps/web/package.json` — 如需补充 Markdown 渲染依赖

### Validation / docs
- Modify: `docs/superpowers/specs/2026-03-26-chat-mvp-design.md` — 如实施中发现必须澄清的小偏差，再回写；正常情况下不改
- Use during validation: `agent-browser` skill — 最后一轮真实浏览器联调

---

### Task 1: 扩展 shared chat 契约

**Files:**
- Create: `packages/shared/src/chat.ts`
- Modify: `packages/shared/src/index.ts`
- Test: `pnpm --filter @ai-chat/web test -- chat-store.test.ts`

- [ ] **Step 1: 写共享 chat 类型文件**

```ts
export type ChatMessageRole = 'USER' | 'ASSISTANT' | 'SYSTEM';

export interface ChatSessionSummary {
  id: string;
  title: string;
  model: string;
  createdAt: string;
  updatedAt: string;
}

export interface ChatMessage {
  id: string;
  sessionId: string;
  role: ChatMessageRole;
  content: string;
  createdAt: string;
}

export interface ListChatSessionsResponse {
  sessions: ChatSessionSummary[];
}

export interface GetChatMessagesResponse {
  session: ChatSessionSummary;
  messages: ChatMessage[];
}

export interface SendChatMessageRequest {
  sessionId?: string;
  content: string;
}

export interface ChatStreamStartedEvent {
  type: 'started';
  session: ChatSessionSummary;
  userMessage: ChatMessage;
}

export interface ChatStreamDeltaEvent {
  type: 'delta';
  delta: string;
}

export interface ChatStreamCompletedEvent {
  type: 'completed';
  session: ChatSessionSummary;
  message: ChatMessage;
}

export interface ChatStreamErrorEvent {
  type: 'error';
  message: string;
}

export type ChatStreamEvent =
  | ChatStreamStartedEvent
  | ChatStreamDeltaEvent
  | ChatStreamCompletedEvent
  | ChatStreamErrorEvent;
```

- [ ] **Step 2: 导出 chat 类型**

```ts
export * from './auth';
export * from './user';
export * from './chat';
```

- [ ] **Step 3: 运行一个会消费 shared 的前端测试，确认类型导出未破坏现有工程**

Run: `pnpm --filter @ai-chat/web test -- protected-route.test.tsx`
Expected: PASS，现有认证路由测试继续通过

- [ ] **Step 4: 提交当前变更**

```bash
git add packages/shared/src/chat.ts packages/shared/src/index.ts
git commit -m "feat: add shared chat contracts"
```

---

### Task 2: 新增 Prisma 聊天数据模型

**Files:**
- Modify: `apps/api/prisma/schema.prisma`
- Create: `apps/api/prisma/migrations/<timestamp>_add_chat_models/migration.sql`
- Test: `apps/api/prisma/schema.prisma`

- [ ] **Step 1: 先写会失败的 schema 目标内容**

```prisma
enum ChatMessageRole {
  USER
  ASSISTANT
  SYSTEM
}

model ChatSession {
  id        String        @id @default(cuid())
  userId    String
  title     String
  model     String
  createdAt DateTime      @default(now())
  updatedAt DateTime      @updatedAt
  user      User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages  ChatMessage[]

  @@index([userId, updatedAt])
}

model ChatMessage {
  id        String          @id @default(cuid())
  sessionId String
  role      ChatMessageRole
  content   String
  createdAt DateTime        @default(now())
  session   ChatSession     @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId, createdAt])
}
```

并把 `User` 模型补成：

```prisma
model User {
  id           String        @id @default(cuid())
  email        String        @unique
  passwordHash String
  role         UserRole      @default(USER)
  status       UserStatus    @default(ACTIVE)
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  chatSessions ChatSession[]
}
```

- [ ] **Step 2: 运行 Prisma 校验，确认当前 schema 从“无聊天模型”变为可生成状态**

Run: `pnpm --filter @ai-chat/api exec prisma validate`
Expected: `The schema at apps/api/prisma/schema.prisma is valid`

- [ ] **Step 3: 生成迁移文件**

Run: `pnpm --filter @ai-chat/api db:migrate --name add-chat-models`
Expected: 生成 `apps/api/prisma/migrations/*_add_chat_models/migration.sql`

- [ ] **Step 4: 检查迁移 SQL 必须只包含聊天表、索引和枚举新增**

迁移文件应包含与下面等价的 SQL：

```sql
CREATE TYPE "ChatMessageRole" AS ENUM ('USER', 'ASSISTANT', 'SYSTEM');

CREATE TABLE "ChatSession" (
  "id" TEXT NOT NULL,
  "userId" TEXT NOT NULL,
  "title" TEXT NOT NULL,
  "model" TEXT NOT NULL,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP(3) NOT NULL,
  CONSTRAINT "ChatSession_pkey" PRIMARY KEY ("id")
);

CREATE TABLE "ChatMessage" (
  "id" TEXT NOT NULL,
  "sessionId" TEXT NOT NULL,
  "role" "ChatMessageRole" NOT NULL,
  "content" TEXT NOT NULL,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT "ChatMessage_pkey" PRIMARY KEY ("id")
);

CREATE INDEX "ChatSession_userId_updatedAt_idx" ON "ChatSession"("userId", "updatedAt");
CREATE INDEX "ChatMessage_sessionId_createdAt_idx" ON "ChatMessage"("sessionId", "createdAt");

ALTER TABLE "ChatSession"
ADD CONSTRAINT "ChatSession_userId_fkey"
FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;

ALTER TABLE "ChatMessage"
ADD CONSTRAINT "ChatMessage_sessionId_fkey"
FOREIGN KEY ("sessionId") REFERENCES "ChatSession"("id") ON DELETE CASCADE ON UPDATE CASCADE;
```

- [ ] **Step 5: 提交当前变更**

```bash
git add apps/api/prisma/schema.prisma apps/api/prisma/migrations
git commit -m "feat: add chat persistence models"
```

---

### Task 3: 建立 llm 模块与 DeepSeek 流式调用边界

**Files:**
- Modify: `apps/api/src/common/config/env.ts`
- Create: `apps/api/src/modules/llm/llm.module.ts`
- Create: `apps/api/src/modules/llm/llm.service.ts`
- Create: `apps/api/src/modules/llm/llm.types.ts`
- Modify: `apps/api/package.json`
- Modify: `.env.example`
- Test: `apps/api/src/modules/run/run.service.spec.ts`

- [ ] **Step 1: 补充环境变量定义**

在 `apps/api/src/common/config/env.ts` 中加入：

```ts
import { z } from 'zod';

export const envSchema = z.object({
  DATABASE_URL: z.string().min(1),
  REDIS_URL: z.string().min(1),
  JWT_SECRET: z.string().min(1),
  API_PORT: z.coerce.number().default(3000),
  DEEPSEEK_API_KEY: z.string().min(1),
  DEEPSEEK_BASE_URL: z.string().url().optional(),
  DEEPSEEK_MODEL: z.string().min(1).default('deepseek-chat')
});

export type Env = z.infer<typeof envSchema>;
export const env = envSchema.parse(process.env);
```

- [ ] **Step 2: 把 `.env.example` 扩成 Chat MVP 所需变量**

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ai_chat"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me"
API_PORT=3000
WEB_PORT=5173
VITE_API_BASE_URL="http://localhost:3000"
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin123456"
DEEPSEEK_API_KEY=""
DEEPSEEK_BASE_URL="https://api.deepseek.com"
DEEPSEEK_MODEL="deepseek-chat"
```

- [ ] **Step 3: 安装或声明 DeepSeek/OpenAI 兼容 SDK 依赖**

`apps/api/package.json` 依赖段应新增一项，例如：

```json
"openai": "^4.86.1"
```

随后执行：

Run: `pnpm install`
Expected: lockfile 更新且 `openai` 安装成功

- [ ] **Step 4: 写 llm 流事件类型**

`apps/api/src/modules/llm/llm.types.ts`：

```ts
export interface LlmMessage {
  role: 'system' | 'user' | 'assistant';
  content: string;
}

export interface LlmStreamDelta {
  type: 'delta';
  text: string;
}

export interface LlmStreamCompleted {
  type: 'completed';
}

export interface LlmStreamFailed {
  type: 'error';
  message: string;
}

export type LlmStreamEvent = LlmStreamDelta | LlmStreamCompleted | LlmStreamFailed;
```

- [ ] **Step 5: 写 llm.service.ts 最小实现**

```ts
import OpenAI from 'openai';
import { Injectable, InternalServerErrorException } from '@nestjs/common';
import { env } from '../../common/config/env';
import type { LlmMessage } from './llm.types';

@Injectable()
export class LlmService {
  private readonly client = new OpenAI({
    apiKey: env.DEEPSEEK_API_KEY,
    baseURL: env.DEEPSEEK_BASE_URL || 'https://api.deepseek.com'
  });

  readonly model = env.DEEPSEEK_MODEL;

  async *streamChat(messages: LlmMessage[]) {
    if (!env.DEEPSEEK_API_KEY) {
      throw new InternalServerErrorException('DeepSeek API key is missing');
    }

    const stream = await this.client.chat.completions.create({
      model: this.model,
      stream: true,
      messages
    });

    for await (const chunk of stream) {
      const text = chunk.choices[0]?.delta?.content;
      if (text) {
        yield { type: 'delta' as const, text };
      }
    }

    yield { type: 'completed' as const };
  }
}
```

- [ ] **Step 6: 注册 llm 模块**

`apps/api/src/modules/llm/llm.module.ts`：

```ts
import { Module } from '@nestjs/common';
import { LlmService } from './llm.service';

@Module({
  providers: [LlmService],
  exports: [LlmService]
})
export class LlmModule {}
```

- [ ] **Step 7: 用 run 单测占位验证 llm 模块边界可被消费**

先写 `apps/api/src/modules/run/run.service.spec.ts` 的测试骨架：

```ts
import { describe, expect, it } from '@jest/globals';

describe('RunService', () => {
  it('placeholder for llm stream integration', () => {
    expect(true).toBe(true);
  });
});
```

Run: `pnpm --filter @ai-chat/api test -- run.service.spec.ts`
Expected: PASS，证明新依赖与模块基础文件可被 Jest 装载

- [ ] **Step 8: 提交当前变更**

```bash
git add .env.example apps/api/package.json pnpm-lock.yaml apps/api/src/common/config/env.ts apps/api/src/modules/llm apps/api/src/modules/run/run.service.spec.ts
git commit -m "feat: add DeepSeek llm module"
```

---

### Task 4: 建立 run 模块并验证上下文拼装

**Files:**
- Create: `apps/api/src/modules/run/run.module.ts`
- Create: `apps/api/src/modules/run/run.service.ts`
- Modify: `apps/api/src/modules/run/run.service.spec.ts`
- Test: `apps/api/src/modules/run/run.service.spec.ts`

- [ ] **Step 1: 先写 run service 单测，约束历史消息到 llm messages 的转换**

```ts
import { describe, expect, it, vi } from '@jest/globals';
import { RunService } from './run.service';

const llmService = {
  streamChat: vi.fn()
};

describe('RunService', () => {
  it('maps chat history into llm messages and yields llm events', async () => {
    llmService.streamChat.mockImplementation(async function* () {
      yield { type: 'delta', text: 'Hello' };
      yield { type: 'completed' };
    });

    const service = new RunService(llmService as never);
    const result: Array<{ type: string; text?: string }> = [];

    for await (const event of service.streamReply({
      history: [
        { role: 'USER', content: 'Hi' },
        { role: 'ASSISTANT', content: 'Hello, how can I help?' }
      ],
      prompt: 'Tell me a joke'
    })) {
      result.push(event);
    }

    expect(llmService.streamChat).toHaveBeenCalledWith([
      { role: 'user', content: 'Hi' },
      { role: 'assistant', content: 'Hello, how can I help?' },
      { role: 'user', content: 'Tell me a joke' }
    ]);
    expect(result).toEqual([
      { type: 'delta', text: 'Hello' },
      { type: 'completed' }
    ]);
  });
});
```

- [ ] **Step 2: 运行测试，确认当前因缺少 `RunService` 失败**

Run: `pnpm --filter @ai-chat/api test -- run.service.spec.ts`
Expected: FAIL，提示 `Cannot find module './run.service'` 或 `RunService is not defined`

- [ ] **Step 3: 实现 run.service.ts**

```ts
import { Injectable } from '@nestjs/common';
import { LlmService } from '../llm/llm.service';

interface HistoryMessage {
  role: 'USER' | 'ASSISTANT' | 'SYSTEM';
  content: string;
}

interface StreamReplyInput {
  history: HistoryMessage[];
  prompt: string;
}

@Injectable()
export class RunService {
  constructor(private readonly llmService: LlmService) {}

  async *streamReply(input: StreamReplyInput) {
    const messages = [
      ...input.history.map((message) => ({
        role:
          message.role === 'ASSISTANT'
            ? ('assistant' as const)
            : message.role === 'SYSTEM'
              ? ('system' as const)
              : ('user' as const),
        content: message.content
      })),
      { role: 'user' as const, content: input.prompt }
    ];

    for await (const event of this.llmService.streamChat(messages)) {
      yield event;
    }
  }
}
```

- [ ] **Step 4: 注册 run 模块**

`apps/api/src/modules/run/run.module.ts`：

```ts
import { Module } from '@nestjs/common';
import { LlmModule } from '../llm/llm.module';
import { RunService } from './run.service';

@Module({
  imports: [LlmModule],
  providers: [RunService],
  exports: [RunService]
})
export class RunModule {}
```

- [ ] **Step 5: 重新运行测试确认通过**

Run: `pnpm --filter @ai-chat/api test -- run.service.spec.ts`
Expected: PASS

- [ ] **Step 6: 提交当前变更**

```bash
git add apps/api/src/modules/run/run.module.ts apps/api/src/modules/run/run.service.ts apps/api/src/modules/run/run.service.spec.ts
git commit -m "feat: add chat run orchestration"
```

---

### Task 5: 建立 chat 模块的会话与消息查询能力

**Files:**
- Modify: `apps/api/src/app.module.ts`
- Create: `apps/api/src/modules/chat/chat.module.ts`
- Create: `apps/api/src/modules/chat/chat.service.ts`
- Create: `apps/api/src/modules/chat/chat.controller.ts`
- Create: `apps/api/src/modules/chat/chat.types.ts`
- Create: `apps/api/src/modules/chat/dto/create-chat-message.dto.ts`
- Test: `apps/api/test/chat.e2e-spec.ts`

- [ ] **Step 1: 先写 e2e 测试，约束会话列表只返回当前用户自己的数据**

```ts
it('GET /chat/sessions returns only current user sessions', async () => {
  const user1 = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'chat1@example.com', password: 'password123' })
    .expect(201);

  const user2 = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'chat2@example.com', password: 'password123' })
    .expect(201);

  const user1Id = user1.body.user.id;
  const user2Id = user2.body.user.id;

  await prisma.chatSession.createMany({
    data: [
      { id: 'session-1', userId: user1Id, title: 'Mine', model: 'deepseek-chat' },
      { id: 'session-2', userId: user2Id, title: 'Not mine', model: 'deepseek-chat' }
    ]
  });

  const response = await request(app.getHttpServer())
    .get('/chat/sessions')
    .set('Authorization', `Bearer ${user1.body.accessToken}`)
    .expect(200);

  expect(response.body.sessions).toEqual([
    expect.objectContaining({ id: 'session-1', title: 'Mine' })
  ]);
});
```

- [ ] **Step 2: 再写 e2e 测试，约束历史消息接口做用户隔离**

```ts
it('GET /chat/sessions/:sessionId/messages rejects sessions from another user', async () => {
  const owner = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'owner@example.com', password: 'password123' })
    .expect(201);

  const other = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'other@example.com', password: 'password123' })
    .expect(201);

  await prisma.chatSession.create({
    data: {
      id: 'private-session',
      userId: owner.body.user.id,
      title: 'Private',
      model: 'deepseek-chat'
    }
  });

  await request(app.getHttpServer())
    .get('/chat/sessions/private-session/messages')
    .set('Authorization', `Bearer ${other.body.accessToken}`)
    .expect(404);
});
```

- [ ] **Step 3: 运行测试，确认当前因没有 chat 模块失败**

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: FAIL，提示 `/chat/sessions` 404 或 `prisma.chatSession` 类型不存在（若先写测试后未生成 client，则先运行 `pnpm --filter @ai-chat/api db:generate`）

- [ ] **Step 4: 写 DTO 与内部类型**

`apps/api/src/modules/chat/dto/create-chat-message.dto.ts`：

```ts
import { IsOptional, IsString, MinLength } from 'class-validator';

export class CreateChatMessageDto {
  @IsOptional()
  @IsString()
  sessionId?: string;

  @IsString()
  @MinLength(1)
  content!: string;
}
```

`apps/api/src/modules/chat/chat.types.ts`：

```ts
export interface ChatSessionRecord {
  id: string;
  userId: string;
  title: string;
  model: string;
  createdAt: Date;
  updatedAt: Date;
}
```

- [ ] **Step 5: 实现 chat.service.ts 的查询与辅助方法**

```ts
import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../../common/prisma/prisma.service';
import { env } from '../../common/config/env';

@Injectable()
export class ChatService {
  constructor(private readonly prisma: PrismaService) {}

  async listSessions(userId: string) {
    return this.prisma.chatSession.findMany({
      where: { userId },
      orderBy: { updatedAt: 'desc' }
    });
  }

  async getSessionOrThrow(userId: string, sessionId: string) {
    const session = await this.prisma.chatSession.findFirst({
      where: { id: sessionId, userId }
    });

    if (!session) {
      throw new NotFoundException('Chat session not found');
    }

    return session;
  }

  async listMessages(userId: string, sessionId: string) {
    const session = await this.getSessionOrThrow(userId, sessionId);
    const messages = await this.prisma.chatMessage.findMany({
      where: { sessionId },
      orderBy: { createdAt: 'asc' }
    });

    return { session, messages };
  }

  getDefaultModel() {
    return env.DEEPSEEK_MODEL;
  }

  buildTitleFromFirstMessage(content: string) {
    return content.trim().slice(0, 40) || 'New Chat';
  }
}
```

- [ ] **Step 6: 实现 chat.controller.ts 的查询接口并挂载鉴权**

```ts
import { Controller, Get, Param, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../../common/guards/jwt-auth.guard';
import { CurrentUser } from '../../common/decorators/current-user.decorator';
import { ChatService } from './chat.service';

@Controller('chat')
@UseGuards(JwtAuthGuard)
export class ChatController {
  constructor(private readonly chatService: ChatService) {}

  @Get('sessions')
  async listSessions(@CurrentUser() user: { userId: string }) {
    const sessions = await this.chatService.listSessions(user.userId);
    return { sessions };
  }

  @Get('sessions/:sessionId/messages')
  async listMessages(
    @CurrentUser() user: { userId: string },
    @Param('sessionId') sessionId: string
  ) {
    return this.chatService.listMessages(user.userId, sessionId);
  }
}
```

- [ ] **Step 7: 注册 chat 模块并接入 AppModule**

`apps/api/src/modules/chat/chat.module.ts`：

```ts
import { Module } from '@nestjs/common';
import { PrismaModule } from '../../common/prisma/prisma.module';
import { ChatController } from './chat.controller';
import { ChatService } from './chat.service';

@Module({
  imports: [PrismaModule],
  controllers: [ChatController],
  providers: [ChatService],
  exports: [ChatService]
})
export class ChatModule {}
```

`apps/api/src/app.module.ts`：

```ts
imports: [PrismaModule, UsersModule, AuthModule, ChatModule],
```

- [ ] **Step 8: 重新运行 e2e 测试确认通过**

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: PASS，列表与越权访问测试都通过

- [ ] **Step 9: 提交当前变更**

```bash
git add apps/api/src/app.module.ts apps/api/src/modules/chat apps/api/test/chat.e2e-spec.ts
git commit -m "feat: add chat session query endpoints"
```

---

### Task 6: 实现 chat SSE 发送链路与 assistant 落库

**Files:**
- Modify: `apps/api/src/modules/chat/chat.service.ts`
- Modify: `apps/api/src/modules/chat/chat.controller.ts`
- Modify: `apps/api/src/modules/chat/chat.module.ts`
- Modify: `apps/api/test/chat.e2e-spec.ts`
- Test: `apps/api/test/chat.e2e-spec.ts`

- [ ] **Step 1: 先写 e2e 测试，约束首条消息创建会话、保存用户消息并返回 started 事件**

```ts
it('POST /chat/stream creates session from first message and emits started event', async () => {
  const user = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'stream@example.com', password: 'password123' })
    .expect(201);

  const response = await request(app.getHttpServer())
    .post('/chat/stream')
    .set('Authorization', `Bearer ${user.body.accessToken}`)
    .send({ content: 'Explain recursion simply' })
    .expect(200);

  expect(response.text).toContain('event: message');
  expect(response.text).toContain('"type":"started"');
  expect(response.text).toContain('Explain recursion simply');
});
```

- [ ] **Step 2: 再写 e2e 测试，约束流结束后 assistant 消息已落库**

```ts
it('POST /chat/stream persists assistant message after stream completes', async () => {
  const user = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'persist@example.com', password: 'password123' })
    .expect(201);

  await request(app.getHttpServer())
    .post('/chat/stream')
    .set('Authorization', `Bearer ${user.body.accessToken}`)
    .send({ content: 'Say hello' })
    .expect(200);

  const session = await prisma.chatSession.findFirstOrThrow({
    where: { userId: user.body.user.id }
  });
  const messages = await prisma.chatMessage.findMany({
    where: { sessionId: session.id },
    orderBy: { createdAt: 'asc' }
  });

  expect(messages[0]).toEqual(expect.objectContaining({ role: 'USER', content: 'Say hello' }));
  expect(messages[1]).toEqual(expect.objectContaining({ role: 'ASSISTANT' }));
  expect(messages[1].content.length).toBeGreaterThan(0);
});
```

- [ ] **Step 3: 运行测试，确认当前发送链路未实现而失败**

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: FAIL，提示 `/chat/stream` 404

- [ ] **Step 4: 在 chat.service.ts 中实现持久化与发送编排**

新增方法骨架：

```ts
async createSessionWithFirstMessage(userId: string, content: string) {
  const session = await this.prisma.chatSession.create({
    data: {
      userId,
      title: this.buildTitleFromFirstMessage(content),
      model: this.getDefaultModel()
    }
  });

  const userMessage = await this.prisma.chatMessage.create({
    data: {
      sessionId: session.id,
      role: 'USER',
      content
    }
  });

  return { session, userMessage };
}

async addUserMessage(sessionId: string, content: string) {
  return this.prisma.chatMessage.create({
    data: { sessionId, role: 'USER', content }
  });
}

async saveAssistantMessage(sessionId: string, content: string) {
  const message = await this.prisma.chatMessage.create({
    data: { sessionId, role: 'ASSISTANT', content }
  });

  await this.prisma.chatSession.update({
    where: { id: sessionId },
    data: { updatedAt: new Date() }
  });

  return message;
}
```

- [ ] **Step 5: 在 chat.module.ts 中引入 run 模块**

```ts
imports: [PrismaModule, RunModule],
```

- [ ] **Step 6: 实现 chat.controller.ts 的 `/chat/stream` SSE 接口**

```ts
import { Body, Controller, Get, Param, Post, Res, UseGuards } from '@nestjs/common';
import type { Response } from 'express';
import { RunService } from '../run/run.service';
import { CreateChatMessageDto } from './dto/create-chat-message.dto';

@Post('stream')
async streamChat(
  @CurrentUser() user: { userId: string },
  @Body() dto: CreateChatMessageDto,
  @Res() res: Response
) {
  res.setHeader('Content-Type', 'text/event-stream');
  res.setHeader('Cache-Control', 'no-cache');
  res.setHeader('Connection', 'keep-alive');

  const content = dto.content.trim();
  let session;
  let userMessage;

  if (dto.sessionId) {
    session = await this.chatService.getSessionOrThrow(user.userId, dto.sessionId);
    userMessage = await this.chatService.addUserMessage(session.id, content);
  } else {
    ({ session, userMessage } = await this.chatService.createSessionWithFirstMessage(user.userId, content));
  }

  res.write(`event: message\ndata: ${JSON.stringify({ type: 'started', session, userMessage })}\n\n`);

  const historyResult = await this.chatService.listMessages(user.userId, session.id);
  const history = historyResult.messages.slice(0, -1);
  let assistantText = '';

  try {
    for await (const event of this.runService.streamReply({ history, prompt: content })) {
      if (event.type === 'delta') {
        assistantText += event.text;
        res.write(`event: message\ndata: ${JSON.stringify({ type: 'delta', delta: event.text })}\n\n`);
      }
    }

    const assistantMessage = await this.chatService.saveAssistantMessage(session.id, assistantText);
    const refreshedSession = await this.chatService.getSessionOrThrow(user.userId, session.id);
    res.write(`event: message\ndata: ${JSON.stringify({ type: 'completed', session: refreshedSession, message: assistantMessage })}\n\n`);
    res.end();
  } catch {
    res.write(`event: message\ndata: ${JSON.stringify({ type: 'error', message: 'Chat stream failed' })}\n\n`);
    res.end();
  }
}
```

- [ ] **Step 7: 若 e2e 需要稳定输出，先在测试里 mock `LlmService`**

在 `chat.e2e-spec.ts` 的测试模块里覆盖 provider：

```ts
.overrideProvider(LlmService)
.useValue({
  model: 'deepseek-chat',
  async *streamChat() {
    yield { type: 'delta', text: 'Hello from mock assistant.' };
    yield { type: 'completed' };
  }
})
```

- [ ] **Step 8: 重新运行 e2e 测试确认 started / completed / 落库链路通过**

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: PASS

- [ ] **Step 9: 提交当前变更**

```bash
git add apps/api/src/modules/chat apps/api/test/chat.e2e-spec.ts
git commit -m "feat: add chat stream endpoint"
```

---

### Task 7: 补齐 API 错误路径与越权/缺配验证

**Files:**
- Modify: `apps/api/test/chat.e2e-spec.ts`
- Modify: `apps/api/src/modules/chat/chat.controller.ts`
- Modify: `apps/api/src/modules/llm/llm.service.ts`
- Test: `apps/api/test/chat.e2e-spec.ts`

- [ ] **Step 1: 新增 e2e 测试，约束空消息返回 400**

```ts
it('POST /chat/stream rejects blank content', async () => {
  const user = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'blank@example.com', password: 'password123' })
    .expect(201);

  await request(app.getHttpServer())
    .post('/chat/stream')
    .set('Authorization', `Bearer ${user.body.accessToken}`)
    .send({ content: '' })
    .expect(400);
});
```

- [ ] **Step 2: 新增 e2e 测试，约束模型错误时返回 SSE error 且不保存 assistant 消息**

```ts
it('POST /chat/stream emits error event and does not persist assistant on llm failure', async () => {
  failingLlm = true;

  const user = await request(app.getHttpServer())
    .post('/auth/register')
    .send({ email: 'llmfail@example.com', password: 'password123' })
    .expect(201);

  const response = await request(app.getHttpServer())
    .post('/chat/stream')
    .set('Authorization', `Bearer ${user.body.accessToken}`)
    .send({ content: 'Trigger failure' })
    .expect(200);

  expect(response.text).toContain('"type":"error"');

  const session = await prisma.chatSession.findFirstOrThrow({
    where: { userId: user.body.user.id }
  });
  const messages = await prisma.chatMessage.findMany({ where: { sessionId: session.id } });
  expect(messages).toHaveLength(1);
  expect(messages[0].role).toBe('USER');
});
```

- [ ] **Step 3: 运行测试，确认当前错误路径未完全满足**

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: FAIL，至少有一项错误路径断言不通过

- [ ] **Step 4: 在 controller 中把空消息提前拦成 400**

```ts
if (!content) {
  throw new BadRequestException('Message content is required');
}
```

- [ ] **Step 5: 在 llm.service.ts 中保留缺失 API key 的显式错误**

```ts
if (!env.DEEPSEEK_API_KEY) {
  throw new InternalServerErrorException('DeepSeek API key is missing');
}
```

- [ ] **Step 6: 确认 controller catch 分支不调用 `saveAssistantMessage`**

catch 分支应保持：

```ts
} catch {
  res.write(`event: message\ndata: ${JSON.stringify({ type: 'error', message: 'Chat stream failed' })}\n\n`);
  res.end();
}
```

- [ ] **Step 7: 重新运行 e2e 测试确认通过**

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: PASS

- [ ] **Step 8: 提交当前变更**

```bash
git add apps/api/src/modules/chat/chat.controller.ts apps/api/src/modules/llm/llm.service.ts apps/api/test/chat.e2e-spec.ts
git commit -m "test: cover chat stream error handling"
```

---

### Task 8: 建立前端 chat service 与 store

**Files:**
- Modify: `apps/web/src/lib/api.ts`
- Create: `apps/web/src/services/chat.ts`
- Create: `apps/web/src/stores/chat-store.ts`
- Create: `apps/web/src/__tests__/chat-store.test.ts`
- Test: `apps/web/src/__tests__/chat-store.test.ts`

- [ ] **Step 1: 先写 chat store 单测，约束 started / delta / completed 事件更新状态**

```ts
import { afterEach, describe, expect, it } from 'vitest';
import { useChatStore } from '../stores/chat-store';

afterEach(() => {
  useChatStore.getState().reset();
});

describe('chat-store', () => {
  it('applies started delta and completed events to current session', () => {
    const store = useChatStore.getState();

    store.applyStreamStarted({
      id: 'session-1',
      title: 'Hello',
      model: 'deepseek-chat',
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString()
    }, {
      id: 'msg-user',
      sessionId: 'session-1',
      role: 'USER',
      content: 'Hello',
      createdAt: new Date().toISOString()
    });

    store.applyStreamDelta('Hi');
    store.applyStreamDelta(' there');

    store.applyStreamCompleted({
      id: 'session-1',
      title: 'Hello',
      model: 'deepseek-chat',
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString()
    }, {
      id: 'msg-assistant',
      sessionId: 'session-1',
      role: 'ASSISTANT',
      content: 'Hi there',
      createdAt: new Date().toISOString()
    });

    const state = useChatStore.getState();
    expect(state.currentSessionId).toBe('session-1');
    expect(state.messages).toHaveLength(2);
    expect(state.messages[1].content).toBe('Hi there');
    expect(state.isStreaming).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试，确认当前因缺少 chat store 失败**

Run: `pnpm --filter @ai-chat/web test -- chat-store.test.ts`
Expected: FAIL，提示 `../stores/chat-store` 不存在

- [ ] **Step 3: 改造 apiFetch 支持 token 与 text 响应**

`apps/web/src/lib/api.ts` 改成：

```ts
import { env } from './env';

type ApiFetchOptions = RequestInit & {
  accessToken?: string | null;
  responseType?: 'json' | 'text';
};

export async function apiFetch<T>(path: string, init?: ApiFetchOptions): Promise<T> {
  const response = await fetch(`${env.apiBaseUrl}${path}`, {
    ...init,
    headers: {
      'Content-Type': 'application/json',
      ...(init?.accessToken ? { Authorization: `Bearer ${init.accessToken}` } : {}),
      ...(init?.headers || {})
    }
  });

  if (!response.ok) {
    throw new Error(`Request failed: ${response.status}`);
  }

  if (init?.responseType === 'text') {
    return (await response.text()) as T;
  }

  return response.json() as Promise<T>;
}
```

- [ ] **Step 4: 写 chat service**

`apps/web/src/services/chat.ts`：

```ts
import type {
  GetChatMessagesResponse,
  ListChatSessionsResponse,
  SendChatMessageRequest,
  ChatStreamEvent
} from '@ai-chat/shared';
import { apiFetch } from '../lib/api';

export function listChatSessions(accessToken: string) {
  return apiFetch<ListChatSessionsResponse>('/chat/sessions', { accessToken });
}

export function getChatMessages(accessToken: string, sessionId: string) {
  return apiFetch<GetChatMessagesResponse>(`/chat/sessions/${sessionId}/messages`, { accessToken });
}

export async function streamChatMessage(
  accessToken: string,
  payload: SendChatMessageRequest,
  onEvent: (event: ChatStreamEvent) => void
) {
  const text = await apiFetch<string>('/chat/stream', {
    method: 'POST',
    accessToken,
    body: JSON.stringify(payload),
    responseType: 'text'
  });

  for (const block of text.split('\n\n')) {
    const dataLine = block.split('\n').find((line) => line.startsWith('data: '));
    if (!dataLine) continue;
    onEvent(JSON.parse(dataLine.slice(6)) as ChatStreamEvent);
  }
}
```

- [ ] **Step 5: 实现 chat store 最小状态**

`apps/web/src/stores/chat-store.ts`：

```ts
import { create } from 'zustand';
import type { ChatMessage, ChatSessionSummary } from '@ai-chat/shared';

type ChatState = {
  sessions: ChatSessionSummary[];
  currentSessionId: string | null;
  messages: ChatMessage[];
  draft: string;
  isStreaming: boolean;
  error: string | null;
  reset: () => void;
  setSessions: (sessions: ChatSessionSummary[]) => void;
  setCurrentSession: (sessionId: string | null) => void;
  setMessages: (messages: ChatMessage[]) => void;
  setDraft: (draft: string) => void;
  applyStreamStarted: (session: ChatSessionSummary, userMessage: ChatMessage) => void;
  applyStreamDelta: (delta: string) => void;
  applyStreamCompleted: (session: ChatSessionSummary, message: ChatMessage) => void;
  applyStreamError: (message: string) => void;
};

const emptyAssistantMessage = (sessionId: string): ChatMessage => ({
  id: '__streaming__',
  sessionId,
  role: 'ASSISTANT',
  content: '',
  createdAt: new Date().toISOString()
});

export const useChatStore = create<ChatState>((set, get) => ({
  sessions: [],
  currentSessionId: null,
  messages: [],
  draft: '',
  isStreaming: false,
  error: null,
  reset: () => set({ sessions: [], currentSessionId: null, messages: [], draft: '', isStreaming: false, error: null }),
  setSessions: (sessions) => set({ sessions }),
  setCurrentSession: (currentSessionId) => set({ currentSessionId }),
  setMessages: (messages) => set({ messages }),
  setDraft: (draft) => set({ draft }),
  applyStreamStarted: (session, userMessage) => set((state) => ({
    currentSessionId: session.id,
    sessions: [session, ...state.sessions.filter((item) => item.id !== session.id)],
    messages: [userMessage, emptyAssistantMessage(session.id)],
    isStreaming: true,
    error: null,
    draft: ''
  })),
  applyStreamDelta: (delta) => set((state) => ({
    messages: state.messages.map((message) =>
      message.id === '__streaming__' ? { ...message, content: `${message.content}${delta}` } : message
    )
  })),
  applyStreamCompleted: (session, message) => set((state) => ({
    sessions: [session, ...state.sessions.filter((item) => item.id !== session.id)],
    messages: state.messages.map((item) => (item.id === '__streaming__' ? message : item)),
    isStreaming: false
  })),
  applyStreamError: (message) => set({ isStreaming: false, error: message })
}));
```

- [ ] **Step 6: 重新运行 chat store 单测确认通过**

Run: `pnpm --filter @ai-chat/web test -- chat-store.test.ts`
Expected: PASS

- [ ] **Step 7: 提交当前变更**

```bash
git add apps/web/src/lib/api.ts apps/web/src/services/chat.ts apps/web/src/stores/chat-store.ts apps/web/src/__tests__/chat-store.test.ts
git commit -m "feat: add frontend chat state"
```

---

### Task 9: 搭建 `/chat` 页面与组件树

**Files:**
- Modify: `apps/web/src/router/index.tsx`
- Create: `apps/web/src/pages/chat/ChatPage.tsx`
- Create: `apps/web/src/components/chat/SessionSidebar.tsx`
- Create: `apps/web/src/components/chat/SessionList.tsx`
- Create: `apps/web/src/components/chat/MessageList.tsx`
- Create: `apps/web/src/components/chat/MessageItem.tsx`
- Create: `apps/web/src/components/chat/ChatComposer.tsx`
- Create: `apps/web/src/components/chat/EmptyChatState.tsx`
- Modify: `apps/web/package.json`
- Test: `apps/web/src/__tests__/chat-page.test.tsx`

- [ ] **Step 1: 安装 Markdown 渲染依赖**

在 `apps/web/package.json` 中新增：

```json
"react-markdown": "^10.1.0"
```

Run: `pnpm install`
Expected: 安装成功并更新 lockfile

- [ ] **Step 2: 先写页面测试，约束未登录访问 `/chat` 仍跳转 `/login`**

```ts
it('redirects /chat to /login when unauthenticated', async () => {
  render(<RouterProvider router={router} />);
  window.history.pushState({}, '', '/chat');
  expect(await screen.findByText(/登录/i)).toBeInTheDocument();
});
```

- [ ] **Step 3: 再写页面测试，约束空态页面可见**

```ts
it('renders empty state on /chat when there is no session', async () => {
  useAuthStore.getState().setAuth({
    accessToken: 'token-123',
    user: {
      id: 'user-1',
      email: 'user@example.com',
      role: 'USER',
      status: 'ACTIVE',
      createdAt: new Date().toISOString()
    }
  });

  vi.spyOn(chatService, 'listChatSessions').mockResolvedValue({ sessions: [] });

  window.history.pushState({}, '', '/chat');
  render(<RouterProvider router={router} />);

  expect(await screen.findByText('开始一个新的对话')).toBeInTheDocument();
});
```

- [ ] **Step 4: 运行测试，确认当前因 `/chat` 页面未实现而失败**

Run: `pnpm --filter @ai-chat/web test -- chat-page.test.tsx`
Expected: FAIL

- [ ] **Step 5: 写基础组件骨架**

`apps/web/src/components/chat/EmptyChatState.tsx`：

```tsx
export function EmptyChatState() {
  return (
    <div>
      <h2>开始一个新的对话</h2>
      <p>输入你的第一条消息，系统会自动创建会话。</p>
    </div>
  );
}
```

`apps/web/src/components/chat/SessionList.tsx`：

```tsx
import type { ChatSessionSummary } from '@ai-chat/shared';

export function SessionList(props: {
  sessions: ChatSessionSummary[];
  currentSessionId: string | null;
  onSelect: (sessionId: string) => void;
}) {
  return (
    <div>
      {props.sessions.map((session) => (
        <button key={session.id} onClick={() => props.onSelect(session.id)}>
          {session.title}
        </button>
      ))}
    </div>
  );
}
```

`apps/web/src/components/chat/SessionSidebar.tsx`：

```tsx
import type { ChatSessionSummary } from '@ai-chat/shared';
import { SessionList } from './SessionList';

export function SessionSidebar(props: {
  sessions: ChatSessionSummary[];
  currentSessionId: string | null;
  onNewChat: () => void;
  onSelect: (sessionId: string) => void;
}) {
  return (
    <aside>
      <button onClick={props.onNewChat}>New Chat</button>
      <SessionList
        sessions={props.sessions}
        currentSessionId={props.currentSessionId}
        onSelect={props.onSelect}
      />
    </aside>
  );
}
```

- [ ] **Step 6: 写消息展示与输入组件**

`apps/web/src/components/chat/MessageItem.tsx`：

```tsx
import Markdown from 'react-markdown';
import type { ChatMessage } from '@ai-chat/shared';

export function MessageItem({ message }: { message: ChatMessage }) {
  return (
    <div>
      <strong>{message.role === 'USER' ? 'You' : 'Assistant'}</strong>
      <Markdown>{message.content}</Markdown>
    </div>
  );
}
```

`apps/web/src/components/chat/MessageList.tsx`：

```tsx
import type { ChatMessage } from '@ai-chat/shared';
import { MessageItem } from './MessageItem';

export function MessageList({ messages }: { messages: ChatMessage[] }) {
  return (
    <div>
      {messages.map((message) => (
        <MessageItem key={message.id} message={message} />
      ))}
    </div>
  );
}
```

`apps/web/src/components/chat/ChatComposer.tsx`：

```tsx
export function ChatComposer(props: {
  value: string;
  disabled: boolean;
  onChange: (value: string) => void;
  onSubmit: () => void;
}) {
  return (
    <div>
      <textarea
        value={props.value}
        disabled={props.disabled}
        onChange={(event) => props.onChange(event.target.value)}
      />
      <button disabled={props.disabled} onClick={props.onSubmit}>
        Send
      </button>
    </div>
  );
}
```

- [ ] **Step 7: 实现 ChatPage**

```tsx
import { useEffect } from 'react';
import { EmptyChatState } from '../../components/chat/EmptyChatState';
import { SessionSidebar } from '../../components/chat/SessionSidebar';
import { MessageList } from '../../components/chat/MessageList';
import { ChatComposer } from '../../components/chat/ChatComposer';
import { useAuthStore } from '../../stores/auth-store';
import { useChatStore } from '../../stores/chat-store';
import { listChatSessions, getChatMessages } from '../../services/chat';

export function ChatPage() {
  const accessToken = useAuthStore((state) => state.accessToken);
  const {
    sessions,
    currentSessionId,
    messages,
    draft,
    isStreaming,
    setSessions,
    setCurrentSession,
    setMessages,
    setDraft
  } = useChatStore();

  useEffect(() => {
    if (!accessToken) return;

    listChatSessions(accessToken).then(({ sessions }) => {
      setSessions(sessions);
      if (sessions[0]) {
        setCurrentSession(sessions[0].id);
      }
    });
  }, [accessToken, setCurrentSession, setSessions]);

  useEffect(() => {
    if (!accessToken || !currentSessionId) return;

    getChatMessages(accessToken, currentSessionId).then(({ messages }) => {
      setMessages(messages);
    });
  }, [accessToken, currentSessionId, setMessages]);

  return (
    <div>
      <SessionSidebar
        sessions={sessions}
        currentSessionId={currentSessionId}
        onNewChat={() => {
          setCurrentSession(null);
          setMessages([]);
        }}
        onSelect={setCurrentSession}
      />

      <main>
        {messages.length === 0 ? <EmptyChatState /> : <MessageList messages={messages} />}
        <ChatComposer
          value={draft}
          disabled={isStreaming}
          onChange={setDraft}
          onSubmit={() => undefined}
        />
      </main>
    </div>
  );
}
```

- [ ] **Step 8: 修改 router，把 `/chat` 作为登录后入口**

```tsx
import { Navigate, createBrowserRouter } from 'react-router-dom';
import { ChatPage } from '../pages/chat/ChatPage';

export const router = createBrowserRouter([
  { path: '/login', element: <LoginPage /> },
  {
    path: '/',
    element: <ProtectedRoute />,
    children: [
      { index: true, element: <Navigate to="/chat" replace /> },
      { path: 'chat', element: <ChatPage /> },
      { path: 'dashboard', element: <DashboardPage /> }
    ]
  },
  {
    path: '/admin',
    element: <ProtectedRoute />,
    children: [
      {
        element: <RoleRoute role="ADMIN" />,
        children: [{ index: true, element: <AdminPage /> }]
      }
    ]
  }
]);
```

- [ ] **Step 9: 重新运行页面测试确认通过**

Run: `pnpm --filter @ai-chat/web test -- chat-page.test.tsx`
Expected: PASS

- [ ] **Step 10: 提交当前变更**

```bash
git add apps/web/package.json pnpm-lock.yaml apps/web/src/router/index.tsx apps/web/src/pages/chat apps/web/src/components/chat apps/web/src/__tests__/chat-page.test.tsx
git commit -m "feat: add chat page layout"
```

---

### Task 10: 打通前端发送消息、流式展示与会话切换

**Files:**
- Modify: `apps/web/src/pages/chat/ChatPage.tsx`
- Modify: `apps/web/src/services/chat.ts`
- Modify: `apps/web/src/stores/chat-store.ts`
- Modify: `apps/web/src/__tests__/chat-page.test.tsx`
- Test: `apps/web/src/__tests__/chat-page.test.tsx`

- [ ] **Step 1: 先写页面测试，约束发送消息时本地用户消息立即可见并逐步出现 assistant 内容**

```ts
it('shows local user message immediately and streams assistant response', async () => {
  useAuthStore.getState().setAuth({
    accessToken: 'token-123',
    user: {
      id: 'user-1',
      email: 'user@example.com',
      role: 'USER',
      status: 'ACTIVE',
      createdAt: new Date().toISOString()
    }
  });

  vi.spyOn(chatService, 'listChatSessions').mockResolvedValue({ sessions: [] });
  vi.spyOn(chatService, 'streamChatMessage').mockImplementation(async (_token, _payload, onEvent) => {
    onEvent({
      type: 'started',
      session: {
        id: 'session-1',
        title: 'Hello AI',
        model: 'deepseek-chat',
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString()
      },
      userMessage: {
        id: 'msg-user',
        sessionId: 'session-1',
        role: 'USER',
        content: 'Hello AI',
        createdAt: new Date().toISOString()
      }
    });
    onEvent({ type: 'delta', delta: 'Hi' });
    onEvent({ type: 'delta', delta: ' there' });
    onEvent({
      type: 'completed',
      session: {
        id: 'session-1',
        title: 'Hello AI',
        model: 'deepseek-chat',
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString()
      },
      message: {
        id: 'msg-assistant',
        sessionId: 'session-1',
        role: 'ASSISTANT',
        content: 'Hi there',
        createdAt: new Date().toISOString()
      }
    });
  });

  window.history.pushState({}, '', '/chat');
  render(<RouterProvider router={router} />);

  await screen.findByText('开始一个新的对话');
  await userEvent.type(screen.getByRole('textbox'), 'Hello AI');
  await userEvent.click(screen.getByRole('button', { name: 'Send' }));

  expect(await screen.findByText('Hello AI')).toBeInTheDocument();
  expect(await screen.findByText('Hi there')).toBeInTheDocument();
});
```

- [ ] **Step 2: 再写页面测试，约束点击会话列表会刷新消息区**

```ts
it('loads messages when switching sessions', async () => {
  useAuthStore.getState().setAuth({
    accessToken: 'token-123',
    user: {
      id: 'user-1',
      email: 'user@example.com',
      role: 'USER',
      status: 'ACTIVE',
      createdAt: new Date().toISOString()
    }
  });

  vi.spyOn(chatService, 'listChatSessions').mockResolvedValue({
    sessions: [
      {
        id: 'session-1',
        title: 'Session One',
        model: 'deepseek-chat',
        createdAt: new Date().toISOString(),
        updatedAt: new Date().toISOString()
      }
    ]
  });

  vi.spyOn(chatService, 'getChatMessages').mockResolvedValue({
    session: {
      id: 'session-1',
      title: 'Session One',
      model: 'deepseek-chat',
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString()
    },
    messages: [
      {
        id: 'msg-1',
        sessionId: 'session-1',
        role: 'USER',
        content: 'Loaded from API',
        createdAt: new Date().toISOString()
      }
    ]
  });

  window.history.pushState({}, '', '/chat');
  render(<RouterProvider router={router} />);

  expect(await screen.findByText('Loaded from API')).toBeInTheDocument();
});
```

- [ ] **Step 3: 运行测试，确认当前 ChatPage 未打通发送链路而失败**

Run: `pnpm --filter @ai-chat/web test -- chat-page.test.tsx`
Expected: FAIL

- [ ] **Step 4: 扩展 chat store 以支持会话局部切换与本地草稿保留**

把 `setCurrentSession` 改成：

```ts
setCurrentSession: (currentSessionId) => set({ currentSessionId, error: null }),
```

并把 `applyStreamError` 改成：

```ts
applyStreamError: (message) => set((state) => ({
  isStreaming: false,
  error: message,
  draft: state.draft
}))
```

- [ ] **Step 5: 在 ChatPage 中实现发送逻辑与事件分发**

`ChatPage.tsx` 的 `onSubmit` 改成：

```tsx
onSubmit={async () => {
  if (!accessToken || !draft.trim() || isStreaming) {
    return;
  }

  const content = draft.trim();
  setDraft(content);

  await streamChatMessage(
    accessToken,
    currentSessionId ? { sessionId: currentSessionId, content } : { content },
    (event) => {
      if (event.type === 'started') {
        useChatStore.getState().applyStreamStarted(event.session, event.userMessage);
      }
      if (event.type === 'delta') {
        useChatStore.getState().applyStreamDelta(event.delta);
      }
      if (event.type === 'completed') {
        useChatStore.getState().applyStreamCompleted(event.session, event.message);
      }
      if (event.type === 'error') {
        useChatStore.getState().applyStreamError(event.message);
      }
    }
  );
}}
```

并在文件顶部引入：

```tsx
import { streamChatMessage } from '../../services/chat';
```

- [ ] **Step 6: 在会话切换时忽略旧流结果，先用 sessionId 对齐实现最小保护**

把 `applyStreamDelta` 改成仅更新当前会话的 streaming 消息：

```ts
applyStreamDelta: (delta) => set((state) => ({
  messages: state.messages.map((message) =>
    message.id === '__streaming__' && message.sessionId === state.currentSessionId
      ? { ...message, content: `${message.content}${delta}` }
      : message
  )
}))
```

- [ ] **Step 7: 重新运行页面测试确认通过**

Run: `pnpm --filter @ai-chat/web test -- chat-page.test.tsx`
Expected: PASS

- [ ] **Step 8: 提交当前变更**

```bash
git add apps/web/src/pages/chat/ChatPage.tsx apps/web/src/services/chat.ts apps/web/src/stores/chat-store.ts apps/web/src/__tests__/chat-page.test.tsx
git commit -m "feat: wire chat streaming UI"
```

---

### Task 11: 跑全量测试并做本地联调验收

**Files:**
- Modify: 如测试或联调发现小问题，则只改对应文件
- Test: `apps/api/test/chat.e2e-spec.ts`
- Test: `apps/web/src/__tests__/chat-page.test.tsx`

- [ ] **Step 1: 跑 API 单测与 e2e**

Run: `pnpm --filter @ai-chat/api test`
Expected: PASS

Run: `pnpm --filter @ai-chat/api test:e2e`
Expected: PASS

- [ ] **Step 2: 跑 Web 单测**

Run: `pnpm --filter @ai-chat/web test`
Expected: PASS

- [ ] **Step 3: 跑 workspace lint/build，确认 monorepo 维度无回归**

Run: `pnpm lint`
Expected: PASS

Run: `pnpm build`
Expected: PASS

- [ ] **Step 4: 本地启动依赖与应用**

Run: `pnpm db:up`
Expected: PostgreSQL 与 Redis 容器启动成功

Run: `pnpm --filter @ai-chat/api db:generate`
Expected: Prisma Client 生成成功

Run: `pnpm --filter @ai-chat/api db:migrate`
Expected: 聊天相关迁移应用成功

Run: `pnpm --filter @ai-chat/api db:seed`
Expected: 默认管理员创建成功

Run: `pnpm dev`
Expected: API 在 `http://localhost:3000`，Web 在 `http://localhost:5173`

- [ ] **Step 5: 用 agent-browser 做真实浏览器验收**

验证顺序：

```bash
agent-browser open http://localhost:5173/login
agent-browser wait --load networkidle
agent-browser snapshot -i
```

随后执行：
- 用测试账号登录
- 进入 `/chat`
- 点击 `New Chat`
- 发送第一条消息
- 确认 assistant 流式内容出现
- 刷新页面
- 确认会话与历史消息仍存在
- 如有第二个普通用户账号，再验证无法读取他人会话

- [ ] **Step 6: 若联调发现问题，只修最小必需项并重跑对应测试**

修复后至少重跑：

Run: `pnpm --filter @ai-chat/api test:e2e -- chat.e2e-spec.ts`
Expected: PASS

Run: `pnpm --filter @ai-chat/web test -- chat-page.test.tsx`
Expected: PASS

- [ ] **Step 7: 提交最终验收修正**

```bash
git add apps/api apps/web packages/shared .env.example pnpm-lock.yaml
git commit -m "test: validate chat mvp end to end"
```

---

## Self-Review Checklist

### Spec coverage
- 多会话聊天：Task 5、Task 9、Task 10
- 会话与消息持久化：Task 2、Task 6
- DeepSeek 真实模型接入：Task 3
- SSE 流式返回：Task 6、Task 10
- `/chat` 类 ChatGPT 页面：Task 9
- 用户隔离：Task 5、Task 7、Task 11
- Markdown 基础展示：Task 9
- 不新增 Run 表 / 不做 tool / schedule：已在文件结构与任务范围中保持排除

### Placeholder scan
- 已避免使用 TBD / TODO / “自行实现” 类占位
- 每个任务都给出明确文件、命令、期望结果与代码骨架

### Type consistency
- 前后端共享 `ChatSessionSummary`、`ChatMessage`、`ChatStreamEvent`
- 后端消息角色使用 `USER | ASSISTANT | SYSTEM`
- run 模块向 llm 映射为 `user | assistant | system`
- 前端 store/service 页面逻辑统一消费 `started / delta / completed / error`
